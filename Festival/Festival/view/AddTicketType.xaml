<Window x:Class="BADProject.view.AddTicketType"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:context="clr-namespace:BADProject.viewmodel"
        Title="AddTicketType" Height="200" Width="300">
    <Window.DataContext>
        <context:ReserverenVM/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="1*"/>

        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.75*"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" HorizontalAlignment="Center" Margin="0,0,34,0" Width="91" Content="Add ticket type"/>
        <Label Grid.Column="0" Grid.Row="1" Content="Name:"/>
        <Label Grid.Column="0" Grid.Row="2" Content="Price:"/>
        <Label Grid.Column="0" Grid.Row="3" Content="Total:"/>
        <Label Grid.Column="0" Grid.Row="5" Content=""/>

        <!--<ListBox ItemsSource="{Binding EditContact}" DisplayMemberPath="Name"></ListBox>-->
        <Label x:Name="lblOrderID" Content="{Binding Path=SelectedTicket.ID}" Grid.Column="1" HorizontalAlignment="Center" ></Label>
        <TextBox x:Name="txtName"  Grid.Column="1" Grid.Row="1" HorizontalAlignment="Stretch"/>
        <TextBox x:Name="txtPrice" Grid.Column="1" Grid.Row="2"></TextBox>

        <!--by ref selectedItem-->
        <TextBox x:Name="txtTotal" Grid.Column="1" Grid.Row="3"/>

        <Button x:Name="sendBtn" Command="{Binding AddTypeAction}"  Grid.ColumnSpan="2" Grid.Row="9" Click="sendBtn_Click" Content="Add type" />
    </Grid>
</Window>
